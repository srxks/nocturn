/**
 * useGlobalShortcuts (v2)
 *
 * All global shortcuts disabled per v2 specification.
 * Only Spacebar on /timer is permitted (handled locally in Timer.jsx).
 */
export function useGlobalShortcuts() {
  // Global shortcuts intentionally disabled
}

export default useGlobalShortcuts

